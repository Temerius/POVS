# file: postgresql/tasks/extensions_install.yml

- name: PostgreSQL | Ensure PostgreSQL is running
  service:
    name: "{{ postgresql_service_name }}"
    state: started

- name: PostgreSQL | Add extensions to the databases
  become: yes
  become_user: "{{ postgresql_admin_user }}"
  postgresql_ext:
    db: "{{ item.0.db }}"
    schema: extensions
    login_user: "{{ postgresql_service_user }}"
    port: "{{ postgresql_port }}"
    name: "{{ item.1 }}"
  with_subelements:
    - "{{ postgresql_database_extensions }}"
    - extensions
  ignore_errors: "{{ ansible_check_mode }}"
  when: postgresql_database_extensions is defined

- name: PostgreSQL | Add hstore to the databases with the requirement
  become: yes
  become_user: "{{postgresql_service_user}}"
  shell: "{{ postgresql_bin_directory}}/psql {{item.name}} --port={{ postgresql_port | int }} --username {{postgresql_admin_user}} -c 'CREATE EXTENSION IF NOT EXISTS hstore WITH SCHEMA extensions;'"
  with_items: "{{postgresql_databases}}"
  register: hstore_ext_result
  failed_when: hstore_ext_result.rc != 0 and ("already exists, skipping" not in hstore_ext_result.stderr)
  changed_when: hstore_ext_result.rc == 0 and ("already exists, skipping" not in hstore_ext_result.stderr)
  when: item.hstore is defined and item.hstore

- name: PostgreSQL | Add uuid-ossp to the database with the requirement
  become: yes
  become_user: "{{postgresql_service_user}}"
  shell: "{{ postgresql_bin_directory}}/psql {{item.name}} --port={{ postgresql_port | int }} --username {{postgresql_admin_user}} -c 'CREATE EXTENSION IF NOT EXISTS \"uuid-ossp\" WITH SCHEMA extensions;'"
  with_items: "{{postgresql_databases}}"
  register: uuid_ext_result
  failed_when: uuid_ext_result.rc != 0 and ("already exists, skipping" not in uuid_ext_result.stderr)
  changed_when: uuid_ext_result.rc == 0 and ("already exists, skipping" not in uuid_ext_result.stderr)
  when: item.uuid_ossp is defined and item.uuid_ossp

- name: PostgreSQL | Add postgis to the databases with the requirement
  become: yes
  become_user: "{{postgresql_service_user}}"
  shell: "{{ postgresql_bin_directory}}/psql {{item.name}} --port={{ postgresql_port | int }} --username {{postgresql_admin_user}} -c 'CREATE EXTENSION IF NOT EXISTS postgis;'&&psql {{item.name}} -c 'CREATE EXTENSION IF NOT EXISTS postgis_topology WITH SCHEMA extensions;'"
  with_items: "{{postgresql_databases}}"
  when: item.gis is defined and item.gis

- name: PostgreSQL | add cube to the database with the requirement
  become: yes
  become_user: "{{postgresql_service_user}}"
  shell: "{{ postgresql_bin_directory}}/psql {{item.name}} --port={{ postgresql_port | int }} --username {{ postgresql_admin_user }} -c 'create extension if not exists cube WITH SCHEMA extensions;'"
  with_items: "{{postgresql_databases}}"
  when: item.cube is defined and item.cube

- name: PostgreSQL | Add plpgsql to the database with the requirement
  become: yes
  become_user: "{{postgresql_service_user}}"
  shell: "{{ postgresql_bin_directory}}/psql {{item.name}} --port={{ postgresql_port | int }} --username {{ postgresql_admin_user }} -c 'CREATE EXTENSION IF NOT EXISTS plpgsql WITH SCHEMA extensions;'"
  with_items: "{{postgresql_databases}}"
  when: item.plpgsql is defined and item.plpgsql

- name: PostgreSQL | add earthdistance to the database with the requirement
  become: yes
  become_user: "{{postgresql_service_user}}"
  shell: "{{ postgresql_bin_directory}}/psql {{item.name}} --port={{ postgresql_port | int }} --username {{ postgresql_admin_user }} -c 'create extension if not exists earthdistance WITH SCHEMA extensions;'"
  with_items: "{{postgresql_databases}}"
  when: item.earthdistance is defined and item.earthdistance

- name: PostgreSQL | Add citext to the database with the requirement
  become: yes
  become_user: "{{postgresql_service_user}}"
  shell: "{{ postgresql_bin_directory}}/psql {{item.name}} --port={{ postgresql_port | int }} --username {{postgresql_admin_user}} -c 'CREATE EXTENSION IF NOT EXISTS citext WITH SCHEMA extensions;'"
  with_items: "{{postgresql_databases}}"
  register: citext_ext_result
  failed_when: citext_ext_result.rc != 0 and ("already exists, skipping" not in citext_ext_result.stderr)
  changed_when: citext_ext_result.rc == 0 and ("already exists, skipping" not in citext_ext_result.stderr)
  when: item.citext is defined and item.citext
- name: PostgreSQL | Add timescaledb to the database with the requirement
  become: yes
  become_user: "{{postgresql_service_user}}"
  shell: "{{ postgresql_bin_directory}}/psql {{item.name}} --port={{ postgresql_port | int }} --username {{postgresql_admin_user}} -c 'CREATE EXTENSION IF NOT EXISTS timescaledb WITH SCHEMA extensions;'"
  with_items: "{{postgresql_databases}}"
  register: timescaledb_ext_result
  failed_when: timescaledb_ext_result.rc != 0 and ("already exists, skipping" not in timescaledb_ext_result.stderr)
  changed_when: timescaledb_ext_result.rc == 0 and ("already exists, skipping" not in timescaledb_ext_result.stderr)
  when: item.timescaledb is defined and item.timescaledb
- name: PostgreSQL | Add pg_cron to the database with the requirement
  become: yes
  become_user: "{{postgresql_service_user}}"
  shell: "{{ postgresql_bin_directory}}/psql {{item.name}} --port={{ postgresql_port | int }} --username {{postgresql_admin_user}} -c 'CREATE EXTENSION IF NOT EXISTS pg_cron;'"
  with_items: "{{postgresql_databases}}"
  register: pg_cron_ext_result
  failed_when: pg_cron_ext_result.rc != 0 and ("already exists, skipping" not in pg_cron_ext_result.stderr)
  changed_when: pg_cron_ext_result.rc == 0 and ("already exists, skipping" not in pg_cron_ext_result.stderr)
  when: item.pg_cron is defined and item.pg_cron
